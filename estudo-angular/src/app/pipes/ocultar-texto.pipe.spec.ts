import { OcultarTextoPipe } from './ocultar-texto.pipe';

describe('OcultarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
