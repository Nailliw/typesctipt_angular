import { LimiteTextoPipe } from './limite-texto.pipe';

describe('LimiteTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new LimiteTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
